# Tickonomics local forecast secrets.
# Copy to .env (same directory) and fill in. Sourced by docker-compose.forecast.local.yml.

POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# v6 free data sources (ADR-012). Leave blank to run with whatever sources need no key.
FINNHUB_API_KEY=
ALPHAVANTAGE_API_KEY=
FRED_API_KEY=
