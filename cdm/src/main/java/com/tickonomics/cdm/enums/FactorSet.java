package com.tickonomics.cdm.enums;

/**
 * Fama-French factor dataset identifiers. Each value corresponds to a specific ZIP file in the
 * Ken French Data Library.
 */
public enum FactorSet {
  FACTOR_3,
  FACTOR_5,
  MOMENTUM,
  ST_REVERSAL,
  LT_REVERSAL
}
